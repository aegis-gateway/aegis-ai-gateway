# This file has been consolidated.
# The canonical quickstart Compose file is: demos/00-quickstart/docker-compose.yaml
#
# To start the full quickstart stack:
#   ./quickstart.sh
#
# Or manually:
#   cd demos/00-quickstart && docker compose up --build
